export const featureFlags = {
    // Controls whether overlay windows close when the main app is closed
    // true: overlays close with main app (current behavior)
    // false: overlays stay open when main app is closed
    closeAllWithMain: false
}

